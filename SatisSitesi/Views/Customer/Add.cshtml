@using SatisSitesi.Domain.Entities
@using Microsoft.Extensions.Localization
@using SatisSitesi.Resources
@inject IStringLocalizer<SharedResource> Localizer
@model UserEntity

@{
    ViewData["Title"] = "Yeni Müşteri Ekle";
}

<div class="page-header mb-4">
    <div>
        <h1 class="page-title"><i class="bi bi-person-plus me-2 text-primary"></i> Yeni Müşteri Ekle</h1>
        <p class="page-subtitle">Sisteme manuel olarak yeni bir kullanıcı tanımlayın</p>
    </div>
</div>

<div class="saas-card p-4 mx-auto" style="max-width: 600px; border-radius: 24px;">
    <form asp-action="Add" method="post">
        @Html.AntiForgeryToken()

        <div class="mb-4">
            <label class="form-label fw-bold text-secondary small text-uppercase mb-2">@Localizer["Username"]</label>
            <div class="input-group">
                <span class="input-group-text bg-dark border-secondary border-opacity-25 text-secondary">
                    <i class="bi bi-person"></i>
                </span>
                <input asp-for="Username" class="form-control bg-dark text-light border-secondary border-opacity-25" style="border-radius: 0 12px 12px 0; padding: 0.75rem;" required />
            </div>
            <span asp-validation-for="Username" class="text-danger small"></span>
        </div>

        <div class="mb-4">
            <label class="form-label fw-bold text-secondary small text-uppercase mb-2">@Localizer["User_Email"]</label>
            <div class="input-group">
                <span class="input-group-text bg-dark border-secondary border-opacity-25 text-secondary">
                    <i class="bi bi-envelope"></i>
                </span>
                <input asp-for="Email" type="email" class="form-control bg-dark text-light border-secondary border-opacity-25" style="border-radius: 0 12px 12px 0; padding: 0.75rem;" required />
            </div>
            <span asp-validation-for="Email" class="text-danger small"></span>
        </div>

        <div class="mb-4">
            <label class="form-label fw-bold text-secondary small text-uppercase mb-2">@Localizer["Password"]</label>
            <div class="input-group">
                <span class="input-group-text bg-dark border-secondary border-opacity-25 text-secondary">
                    <i class="bi bi-key"></i>
                </span>
                <input asp-for="Password" type="password" class="form-control bg-dark text-light border-secondary border-opacity-25" style="border-radius: 0 12px 12px 0; padding: 0.75rem;" required />
            </div>
            <span asp-validation-for="Password" class="text-danger small"></span>
        </div>

        <div class="mb-4">
            <label class="form-label fw-bold text-secondary small text-uppercase mb-2">Rol</label>
            <div class="input-group">
                <span class="input-group-text bg-dark border-secondary border-opacity-25 text-secondary">
                    <i class="bi bi-shield-check"></i>
                </span>
                <select asp-for="Role" class="form-select bg-dark text-light border-secondary border-opacity-25" style="border-radius: 0 12px 12px 0; padding: 0.75rem;">
                    <option value="Admin">Admin (Yönetici)</option>
                    <option value="User" selected>User (Müşteri)</option>
                </select>
            </div>
        </div>

        <div class="d-flex gap-3 mt-5">
            <button type="submit" class="btn-primary flex-grow-1" style="border-radius: 12px; padding: 0.8rem;">
                <i class="bi bi-plus-circle me-2"></i>Ekle
            </button>
            <a asp-action="Index" class="btn-secondary flex-grow-1 text-center" style="border-radius: 12px; padding: 0.8rem; text-decoration: none;">
                Vazgeç
            </a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
