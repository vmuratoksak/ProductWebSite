@model List<WebApplication1.Models.Entities.OrderEntity>

@{
    ViewData["Title"] = "Siparişlerim";
}

<h2 class="fw-bold mb-4">📦 Siparişlerim</h2>

@if (!Model.Any())
{
    <div class="alert alert-warning">
        Henüz siparişiniz yok.
    </div>
}
else
{
    foreach (var order in Model)
    {
        <div class="card shadow-lg mb-4"
             style="background:#1e293b;color:#e2e8f0;border:none;border-radius:18px;">

            <div class="card-body">

                <div class="d-flex justify-content-between align-items-center mb-3">

                    <div>
                        <h6 class="text-secondary mb-1">
                            Sipariş Tarihi
                        </h6>
                        <h5 class="fw-bold">
                            @order.CreatedAt.ToString("dd.MM.yyyy HH:mm:ss")
                        </h5>
                    </div>

                    <div class="text-end">
                        <h6 class="text-secondary mb-1">
                            Toplam Tutar
                        </h6>
                        <h4 class="fw-bold text-success">
                            @order.TotalAmount ₺
                        </h4>
                    </div>

                </div>

                <hr style="border-color:#334155;" />

                <div class="mt-3">

                    @foreach (var item in order.Items)
                    {
                        <div class="d-flex justify-content-between align-items-center mb-2">

                            <div>
                                <span class="fw-semibold">
                                    @item.ProductName
                                </span>
                            </div>

                            <div class="text-end text-info">
                                @item.Quantity x @item.Price ₺
                            </div>

                        </div>
                    }

                </div>

            </div>
        </div>
    }
}